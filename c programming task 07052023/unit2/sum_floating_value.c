
void main()
{
    float x, y;
    int z;
    printf("enter ");
    scanf("%f %f", &x, &y);
    z = x + y;
    printf("the  sum of %d \n", z);
    printf("the  value of x %1.3f \n", x);
    printf("the  value of y %1.3f \n", y);
    // printf("the  value of x %f \n", x);
}