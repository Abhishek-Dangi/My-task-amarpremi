void main()
{
    int x, y;
    short int z;
    printf("enter the 6 digt number x, y ");
    scanf("%d %d", &x, &y);
    z = x + y;
    printf("the sum of x and y : %hd", z);

    // printf("enter the 6 digt number");
}